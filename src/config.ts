import { STATUS } from '~/literals';

import { Topic } from '~/types';

export const description = 'React App with Redux and Redux Saga';
export const name = 'React App Login Page';
export const topic: Topic = {
  cached: false,
  data: [],
  message: '',
  status: STATUS.IDLE,
  updatedAt: 0,
};
